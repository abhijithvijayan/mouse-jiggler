[env:esp32-c3-devkitm-1]
platform = espressif32
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200
; ESP32-BLE-Mouse is vendored in lib/ (patched for arduino-esp32 core 3.x:
; std::string -> Arduino String in BLEDevice::init / setValue). Upstream
; T-vK/ESP32-BLE-Mouse is unmaintained and fails to build on core 3.x.
